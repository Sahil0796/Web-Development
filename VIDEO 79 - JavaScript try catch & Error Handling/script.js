let a = prompt("Enter the first number");      // Prompt return us the String.
let b = prompt("Enter the second number");

let sum = parseInt(a) + parseInt(b);           // To get the result in an integer value, use parseint().

console.log("This is the total of your numbers", sum );
